/**
 * Storage API for the IndexedDB.
 */
app.service('idbStorageAPI', function ( $http ) {

    this.addProject = function ( project ) {

    };

    this.deleteProject = function ( projectId ) {

    };

    this.updateProject = function ( project ) {

    };

    this.getProjects = function ( ) {

    };

});
